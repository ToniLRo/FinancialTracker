# Configuración para Docker

# Configuración de base de datos para Docker
spring.datasource.url=jdbc:postgresql://db:5432/miappdb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=user
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configuración de seguridad para Docker
app.security.csrf.enabled=true
app.security.cors.allowed-origins=http://localhost:4200,http://localhost:3000,http://localhost:8080

# Logging para Docker
logging.level.com.tonilr.FinancialTracker=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=DEBUG

# Configuración de servidor
server.port=8080
